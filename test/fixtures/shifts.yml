# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% Week::DAYS.each do |day| %>
  <% Service.kinds.symbolize_keys.keys.each do |service| %>
    <%= service %>_<%= day %>:
      connection: <%= service %>
      day: <%= day %>
      from: '10:00'
      to: '17:00'
  <% end %>
<% end %>
