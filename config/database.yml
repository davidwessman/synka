default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: sync-hours_development

test:
  <<: *default
  database: sync-hours_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
